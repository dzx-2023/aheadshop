<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aheadshop.user.mapper.RoleMapper">

    <select id="selectRolesByUserId" resultType="com.aheadshop.user.domain.po.Role">
        SELECT r.id, r.role_name, r.role_key, r.status, r.create_time, r.update_time
        FROM role r
        INNER JOIN user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>

</mapper>