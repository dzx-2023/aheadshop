server:
  port: 8087

spring:
  application:
    name: aheadshop-admin
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yaml
  config:
    import: nacos:aheadshop-admin.yml

logging:
  level:
    com.aheadshop.admin: debug
